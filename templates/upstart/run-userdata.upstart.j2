# cloudstack-set-guest-sshkey

description "run userdata from CloudStack metadata"

start on (startup and
          started networking and
          starting ssh)

task
exec /usr/share/cloudstack/cloudstack-run-userdata start
