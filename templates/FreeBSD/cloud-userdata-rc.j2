#!/bin/sh
# PROVIDE: cloudstack_run_userdata
# REQUIRE: DAEMON networking
# BEFORE:  LOGIN
# KEYWORD: nojail

. /etc/rc.subr

name=cloudstack_run_userdata
rcvar=cloudstack_run_userdata_enable

command="/usr/share/cloudstack/${name}"

load_rc_config $name
run_rc_command "$1"