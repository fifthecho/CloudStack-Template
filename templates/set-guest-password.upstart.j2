# cloudstack-set-guest-password

description "set root password from CloudStack metadata"

start on started network-services

script
  /usr/share/cloudstack/cloudstack-set-guest-password start
end script
